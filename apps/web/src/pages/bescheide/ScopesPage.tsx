import React from "react";
import PlaceholderPage from "./PlaceholderPage";

export default function BescheideScopesPage() {
  return (
    <PlaceholderPage
      breadcrumbKey="breadcrumb.scopes"
      titleKey="scopes.title"
      placeholderKey="module.scopes.placeholder"
    />
  );
}
