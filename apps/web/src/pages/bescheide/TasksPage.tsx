import React from "react";
import PlaceholderPage from "./PlaceholderPage";

export default function BescheideTasksPage() {
  return (
    <PlaceholderPage
      breadcrumbKey="breadcrumb.tasks"
      titleKey="tasks.title"
      placeholderKey="module.tasks.placeholder"
    />
  );
}
